0.5
* все операции с профилями иономера перенесены в автомат profSM (принадлежит окну градуировки) 
+ словари состояний СВП и манипуляторы к ним вынесены в новый модуль sstate.py
* при нажатии кнопки "расчет калибр. коэффициентов" в окне градуировки иономера,
  после завершения расчета, производится внеочередная запись словаря состояний СВП в файл
  (кроме того, исходные данные и результат расчета сохраняется в лог на уровень persistent)

0.4
* большое кол-во доработок - программа приведена к предрелизному виду
  
0.3 
* перешел на python 3.3.2
+ журнал работы СВП (session\persistent)
+ основная форма пользовательского режима + переход в технологический

0.2
* изменился формат вызова CreateRTUdir - DirID теперь ВОЗВРАЩАЕТСЯ при вызове 